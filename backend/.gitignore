__pycache__/
*.py[cod]
*.pyd

.venv/
venv/
env/
.env
.env.*
!.env.example

.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

build/
dist/
*.egg-info/
models/

.DS_Store
.idea/
.vscode/

*.log
*.tmp
*.swp
*.wav

# Derived from experiments/streaming_flow.py via jupytext. The .py is the source of
# truth: it is diffable and reviewable, an .ipynb with embedded audio is neither
# (one executed run is ~3 MB of base64).
experiments/*.ipynb

# Diagnostic captures (see src/tajwid/capture.py) — a reciter's raw audio.
captures/